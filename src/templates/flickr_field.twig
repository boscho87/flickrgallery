{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("itscoding\\flickrgallery\\assetbundles\\flickrgallery\\FlickrGalleryAsset") %}

{{ forms.textField({
    instructions: 'api.id.instructions'|t('flickr-gallery'),
    id: flickr_id.id,
    name: flickr_id.name,
    placeholder:'72157673777849508',
    value: flickr_id.value}) }}


{{ forms.checkbox({
    label: 'api.fetch.frontend'|t('flickr-gallery'),
    id: load_fe.id,
    name: load_fe.name,
    checked: load_fe.value}) }}